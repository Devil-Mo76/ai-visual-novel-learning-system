# 修复：剧本人物的对话 / 性格提示词未生效

## 结论
代码链路其实是**通的**：`SYSTEM_PROMPT`（含两位角色的完整人设卡 TEACHER_CARD / STUDENT_CARD）在生成剧本时确实作为 system message 发给了云端模型。
真正的问题是**生成出的台词「演」不出人设**——希罗像教科书念白、艾玛只会"诶？/对吗？"敷衍附和。原因是角色卡埋在超长 System 提示最开头，被后面大量"章节/弹题/JSON schema/输出长度"结构性规则 + 数万字资料淹没，模型的"结构化任务注意力"压过了"角色扮演注意力"。

## 改了什么（backend/services/script_engine.py）
1. `SYSTEM_PROMPT` 在「双向表情同步」之后、章节规则之前，新增 **【人设与台词——最优先遵守】**：重申希罗（冷静条理、先打比方再讲、口语引导、关切学生）与艾玛（活泼追问、偶尔装傻、会"诶？/原来是这样！/所以你的意思是…"、用自己话复述）两种口吻，并硬性要求「每句 line.text 必须听得出是谁在说，严禁教科书式念白 / 念稿干讲」。
2. `generate_script` 的 **user_prompt 结尾**（紧邻模型开始输出、显著性最高的位置）追加 **【写台词前请再确认】** 人设提醒，避免被开头超长资料再次稀释。

## 验证
- `py_compile` 通过。
- 用真实 `deepseek-v4-flash` 生成「光合作用光反应」一章做测试，台词已能清晰区分两角色：
  - **teacher**："想象一下，类囊体薄膜就像一条太阳能流水线…别急，水分子被分解的时候…就像你榨果汁，果渣会被丢掉一样。"
  - **student**："诶？那氧气呢？" → "原来是这样！所以你的意思是，光能先把水拆了…把能量塞进ATP和NADPH这两个小盒子里？"

## 提醒
需**重启后端**生效（uvicorn 无 --reload）。重启后重新生成剧本即可看到更有人设、更像两人真实聊天的对话。
